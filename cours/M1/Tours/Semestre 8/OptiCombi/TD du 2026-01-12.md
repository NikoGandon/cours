# Exercice - Construction au plus proche voisin

Dans la classe `VRPSolution`, ajouter une méthode `void NearestNeighbourConstruct()`

Le principe de cette construction est :

- Construire une route vide
- Tant qu'il reste des clients non visités et qui peuvent être ajoutés à la route en termes de capacité
  - Choisir le client le plus proche du dernier noeud visité dans la route et l'ajouter à la route
- S'il reste des clients non visités (donc la route est saturée) alors revenir à l'étape 1 sinon terminer l'algo

| Inst | $f(s)$ |
| ---- | ------ |
| n22  | 589    |
| n33  | 1002   |
| n51  | 688    |
| n101 | 1491   |

Tant que liste_client non vide :
Pour tout client $\in$ liste_client
Si client peut être inséré dans la tournée n_tournée
et que distance[dernier_client] < dist_min alors:
n_client <- client
dist_min <- distance[dernier_site][client]
FinSi
FinPour
Si n_client != 1
insérer n_client après dernier_site dans la tournée n_tournée
supprime dernier_client de la liste client
dist_min <- +80 (??)
dernier_site <- n_client
n_client <- 1
